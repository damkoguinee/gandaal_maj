<ul class="navbar-nav me-auto mb-2 mb-lg-0 justify-content-start">
    {% if personnel.personnel.typeUser == 'personnel' %}
        <li class="nav-item">
            <a class="nav-link active btn btn-danger text-light m-1" aria-current="page" href="{{path("app_gandaal_etablissement_home", {etablissement : etablissement.id})}}">Accueil</a>
        </li>
    {% else %}
        
        <li class="nav-item">
            <a class="nav-link active btn btn-danger text-light m-1" aria-current="page" href="{{path("app_gandaal_personnel", {etablissement : etablissement.id, personnelActif : personnel.id})}}">Accueil</a>
        </li>
    {% endif %}

    <li class="nav-item">
        <a class="nav-link active btn btn-danger text-light m-1" aria-current="page" href="{{path("app_gandaal_personnel_calendar", {etablissement : etablissement.id, personnelActif : personnel.id})}}">Mon planning</a>
    </li>

    <li class="nav-item">
        <a class="nav-link active btn btn-danger text-light m-1" aria-current="page" href="{{path("app_gandaal_personnel_salaire", {etablissement : etablissement.id, personnelActif : personnel.id})}}">Mes Paiements</a>
    </li>
    {% if personnel.personnel.typeUser != 'personnel' %}
        <li class="nav-item">
            <a class="nav-link active btn btn-danger text-light m-1" aria-current="page" href="{{path("app_gandaal_personnel_classe", {etablissement : etablissement.id, personnelActif : personnel.id})}}">Mes Classes</a>
        </li>

        <li class="nav-item">
            <a class="nav-link active btn btn-danger text-light m-1" aria-current="page" href="{{path("app_gandaal_personnel_calendar_jour", {etablissement : etablissement.id, personnelActif : personnel.id})}}">Contrôle de présence</a>
        </li>
    {% endif %}
    
</ul>