<form method="post" action="{{ path('app_gandaal_personnel_devoir_delete', {'id': devoir_eleve.id, 'personnelActif' : personnel.id, 'etablissement' : etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ devoir_eleve.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
