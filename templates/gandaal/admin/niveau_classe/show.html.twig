{% extends 'base.html.twig' %}

{% block title %}niveau_classe détail{% endblock %}

{% block body %}
    <div class="row my-2 mx-0 px-0">
        {% include "gandaal/_common/_nav_admin.html.twig" %}
        <div class="col-sm-12 col-md-10 mx-0 px-0">
            {{ include("gandaal/_common/_messages_flash.html.twig") }}
            <div class="container-fluid py-2 mx-0 px-0" style="min-height: 90vh; background-image: url('{{asset(chemin_images_config)}}fond.jpg');">
                {{include("gandaal/_common/_entete.html.twig")}}
                <table class="table table-bordered table-striped table-hover align-middle">
                    <tbody>
                        <tr>
                            <th>Cursus</th>
                            <td>{{ niveau_classe.cursus.nom|ucwords }}</td>
                        </tr>
                        <tr>
                            <th>Etablissement</th>
                            <td>{{ niveau_classe.cursus.etablissement.lieu|ucfirst }}</td>
                        </tr>
                        <tr>
                            <th>Nom</th>
                            <td>{{ niveau_classe.nom|ucwords }}</td>
                        </tr>
                        <tr>
                            <th>Code</th>
                            <td>{{ niveau_classe.code|ucwords }}</td>
                        </tr>
                        
                    </tbody>
                </table>
                <div class="d-flex justify-content-between my-4">
                    <a class="btn btn-light" href="{{ path('app_gandaal_admin_niveau_classe_index') }}">Retour</a>

                    <a class="btn btn-warning" href="{{ path('app_gandaal_admin_niveau_classe_edit', {'id': niveau_classe.id}) }}">Modifier</a>

                    {{ include('gandaal/admin/niveau_classe/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
