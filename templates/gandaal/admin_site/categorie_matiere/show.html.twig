{% extends 'base.html.twig' %}

{% block title %}categorie_matiere détail{% endblock %}

{% block body %}
    <div class="row my-2 mx-0 px-0">
        {% include "gandaal/_common/_nav_admin_site.html.twig" %}
        <div class="col-sm-12 col-md-10 mx-0 px-0">
            {{ include("gandaal/_common/_messages_flash.html.twig") }}
            <div class="container-fluid py-2 mx-0 px-0" style="min-height: 90vh; background-image: url('{{asset(chemin_images_config)}}fond.jpg');">
                {{include("gandaal/_common/_entete_site.html.twig")}}
                <table class="table table-bordered table-striped table-hover align-middle">
                    <tbody>
                        <tr>
                            <th>Nom</th>
                            <td>{{ categorie_matiere.nom }}</td>
                        </tr>
                        
                    </tbody>
                </table>
                <div class="d-flex justify-content-between my-4">
                    <a class="btn btn-light" href="{{ path('app_gandaal_admin_site_categorie_matiere_index') }}">Retour</a>

                    <a class="btn btn-warning" href="{{ path('app_gandaal_admin_site_categorie_matiere_edit', {'id': categorie_matiere.id}) }}">Modifier</a>

                    {{ include('gandaal/admin_site/categorie_matiere/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}
