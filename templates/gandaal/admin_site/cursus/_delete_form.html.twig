<form method="post" action="{{ path('app_gandaal_admin_site_cursus_delete', {'id': cursus.id, etablissement : etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cursus.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
