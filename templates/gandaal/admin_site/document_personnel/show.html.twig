{% extends 'base.html.twig' %}

{% block title %}DocumentPersonnel{% endblock %}

{% block body %}
    <h1>DocumentPersonnel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_personnel.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ document_personnel.type }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ document_personnel.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gandaal_admin_site_document_personnel_index') }}">back to list</a>

    <a href="{{ path('app_gandaal_admin_site_document_personnel_edit', {'id': document_personnel.id}) }}">edit</a>

    {{ include('gandaal/admin_site/document_personnel/_delete_form.html.twig') }}
{% endblock %}
