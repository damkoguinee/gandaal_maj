{% extends 'base.html.twig' %}

{% block title %}DocumentEnseignant{% endblock %}

{% block body %}
    <h1>DocumentEnseignant</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ document_enseignant.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ document_enseignant.type }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ document_enseignant.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_gandaal_admin_site_document_enseignant_index') }}">back to list</a>

    <a href="{{ path('app_gandaal_admin_site_document_enseignant_edit', {'id': document_enseignant.id}) }}">edit</a>

    {{ include('gandaal/admin_site/document_enseignant/_delete_form.html.twig') }}
{% endblock %}
