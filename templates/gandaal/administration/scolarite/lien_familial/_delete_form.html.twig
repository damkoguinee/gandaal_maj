<form method="post" action="{{ path('app_gandaal_administration_scolarite_lien_familial_delete', {'id': lien_familial.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lien_familial.id) }}">
    <button class="btn">Delete</button>
</form>
