<form method="post" action="{{ path('app_gandaal_administration_scolarite_preinscription_delete', {'id': inscription.id, 'etablissement' :  etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ inscription.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
