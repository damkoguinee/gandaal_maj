<form method="post" action="{{ path('app_gandaal_administration_comptabilite_admin_activite_tarif_delete', {'id': tarif.id, etablissement : etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tarif.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
