<form method="post" action="{{ path('app_gandaal_administration_comptabilite_admin_config_actvite_scolaire_delete', {'id': config_activite_scolaire.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ config_activite_scolaire.id) }}">
    <button class="btn">Delete</button>
</form>
