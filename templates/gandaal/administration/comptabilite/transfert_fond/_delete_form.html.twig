<form method="post" action="{{ path('app_gandaal_administration_comptabilite_transfert_fond_delete', {'id': transfert_fond.id, etablissement : etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfert_fond.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
