<form method="post" action="{{ path('app_gandaal_administration_comptabilite_paiement_eleve_delete', {'id': paiement_eleve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ paiement_eleve.id) }}">
    <button class="btn">Delete</button>
</form>
