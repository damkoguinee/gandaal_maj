<form method="post" action="{{ path('app_gandaal_administration_pedagogie_admin_classe_repartition_delete', {'id': classe_repartition.id, etablissement : etablissement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ classe_repartition.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
